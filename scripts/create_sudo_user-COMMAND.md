Run this command to install the non-root user:
```bash
rm -f create_sudo_user.sh && curl -fsSL -o create_sudo_user.sh https://github.com/StardustCollective/NodeCloud/raw/main/scripts/create_sudo_user.sh && bash create_sudo_user.sh
```
